### Note: update .env later when deploying (do not commit secrets).

# random secret key used to encrypt login tokens
JWT_SECRET=my_super_secret_key_change_me_928374

# location for the SQLite database file
DATABASE_URL=./eventecho.db

# choose a safe port (not 5000)
GATEWAY_PORT=5050
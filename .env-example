# === This is an example environment file. The file you should use instead is typically named ".env". ===

# === Java web token key ===
JWT_SECRET=<your_random_secret_key>

# === Token expiration time (in minutes) ===
TOKEN_EXPIRATION_MINUTES=<token_duration_minutes, e.g., 10080>

# === Local Database (with set password) ===
DATABASE_URL="<your_db_url, e.g., postgresql://postgres:mysecretpassword@localhost:5432/eventecho>"

# === AI Keys ===
OPENAI_API_KEY="<your_openai_api_key>"
GEMINI_API_KEY="<your_gemini_api_key>"

# === Gateway port (if running via backend/gateway/server.py) ===
GATEWAY_PORT=<port_number, e.g., 5050>